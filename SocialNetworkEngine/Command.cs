namespace SocialNetworkEngine
{
    public enum command
    {
        timeline,
        post,
        send_message,
        view_messages,
        follow,
        wall,
        log_out
    }
    public enum resultStatus
    {
        IsFaild,
        IsSuccess,
    }
}
